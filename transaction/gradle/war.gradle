apply plugin: "war"

bootWar {
    mainClassName = "com.example.transaction.TransactionApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
